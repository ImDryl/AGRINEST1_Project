<div class="page-header">
    <h1><i class="bi bi-people"></i> User List</h1>
    <a href="{{ path('app_admin_user_new') }}" class="btn btn-add"><i class="bi bi-plus-circle"></i> Create New User</a>
</div>

<div class="card table-flat-card users-list-card">
    <div class="table-controls" style="padding-bottom:0;">
        <div class="control-group" style="gap:.5rem; flex-wrap:wrap;">
            <span style="font-weight:600;">Filter:</span>
            <a href="{{ path('app_admin_users_index') }}" class="btn users-filter-btn {{ selectedRole|default('all') == 'all' ? 'btn-success' : 'btn-secondary' }}">All</a>
            <a href="{{ path('app_admin_users_index', {'role': 'admin'}) }}" class="btn users-filter-btn {{ selectedRole|default('all') == 'admin' ? 'btn-success' : 'btn-secondary' }}">Admin</a>
            <a href="{{ path('app_admin_users_index', {'role': 'staff'}) }}" class="btn users-filter-btn {{ selectedRole|default('all') == 'staff' ? 'btn-success' : 'btn-secondary' }}">Staff</a>
            <a href="{{ path('app_admin_users_index', {'role': 'user'}) }}" class="btn users-filter-btn {{ selectedRole|default('all') == 'user' ? 'btn-success' : 'btn-secondary' }}">User</a>
        </div>
    </div>
    <div class="table-controls">
        <div class="control-group">
            <label for="pageLength" style="font-weight:600;">Show</label>
            <select id="pageLength">
                <option value="5">5</option>
                <option value="10" selected>10</option>
                <option value="25">25</option>
                <option value="50">50</option>
                <option value="100">100</option>
            </select>
            <span>entries</span>
        </div>
        <div class="control-group">
            <label for="tableSearch" style="font-weight:600;">Search:</label>
            <input id="tableSearch" type="text" placeholder="Type to filter..." style="min-width:220px;">
        </div>
    </div>
    <table id="usersTable" class="styled-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Date Created</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td>
                        {% if 'ROLE_ADMIN' in user.roles %}
                            <span class="badge badge-admin">Admin</span>
                        {% elseif 'ROLE_STAFF' in user.roles %}
                            <span class="badge badge-staff">Staff</span>
                        {% else %}
                            <span class="badge badge-user">User</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if user.createdAt %}
                            {{ user.createdAt|date('Y-m-d H:i') }}
                        {% else %}
                            <span style="color: #999;">N/A</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if user.isActive %}
                            <span class="badge badge-success">Active</span>
                        {% else %}
                            <span class="badge badge-danger">Disabled</span>
                        {% endif %}
                    </td>
                    <td class="table-actions">
                        <a href="{{ path('app_admin_user_edit', {'id': user.id}) }}" class="btn btn-edit icon-btn" title="Edit">
                            <i class="bi bi-pencil"></i>
                        </a>
                        {% if user.isActive %}
                            <form method="post" action="{{ path('app_admin_user_disable', {'id': user.id}) }}" style="display:inline-block;" onsubmit="return confirm('Are you sure you want to disable this user?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('disable' ~ user.id) }}">
                                <button type="submit" class="btn btn-warning icon-btn" title="Disable User">
                                    <i class="bi bi-x-circle"></i>
                                </button>
                            </form>
                        {% else %}
                            <form method="post" action="{{ path('app_admin_user_enable', {'id': user.id}) }}" style="display:inline-block;" onsubmit="return confirm('Are you sure you want to enable this user?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('enable' ~ user.id) }}">
                                <button type="submit" class="btn btn-success icon-btn" title="Enable User">
                                    <i class="bi bi-check-circle"></i>
                                </button>
                            </form>
                        {% endif %}
                        <form method="post" action="{{ path('app_admin_user_delete', {'id': user.id}) }}" style="display:inline-block;" onsubmit="return confirm('Are you sure you want to delete this user? This action cannot be undone.');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                            <button class="btn btn-delete icon-btn" title="Delete">
                                <i class="bi bi-trash"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="no-data">No users found.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <div class="table-footer">
      <div id="pageInfo" class="page-info"></div>
      <div id="pagination"></div>
    </div>
</div>

