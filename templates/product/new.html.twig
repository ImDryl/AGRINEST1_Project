{% extends isAdmin|default(false) ? 'admin/base_admin.html.twig' : 'base.html.twig' %}

{% block title %}Create New Product{% endblock %}

{% block admin_content %}
    {{ include('product/_new_content.html.twig') }}
{% endblock %}

{% block body %}
    {% if isAdmin|default(false) %}
        {{ parent() }}
    {% else %}
        {{ include('product/_new_content.html.twig') }}
    {% endif %}
{% endblock %}
